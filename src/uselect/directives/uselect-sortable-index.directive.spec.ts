import { UselectSortableIndexDirective } from './uselect-sortable-index.directive';

describe('UselectSortableIndexDirective', () => {
  it('should create an instance', () => {
    const directive = new UselectSortableIndexDirective();
    expect(directive).toBeTruthy();
  });
});
