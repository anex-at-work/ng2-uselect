import { UselectSortableDataDirective } from './uselect-sortable-data.directive';

describe('UselectSortableDataDirective', () => {
  it('should create an instance', () => {
    const directive = new UselectSortableDataDirective();
    expect(directive).toBeTruthy();
  });
});
